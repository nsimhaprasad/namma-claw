# ===========================================
# NC-Control Environment Configuration
# ===========================================
# Copy this file to .env and fill in the values
# For Docker: docker compose up -d
# For local dev: npm run dev

# -------------------------------------------
# Database (PostgreSQL)
# -------------------------------------------
# For local development:
# DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/nc

# For Docker (auto-configured via docker-compose.yml):
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=nc

# -------------------------------------------
# Security (REQUIRED - generate these!)
# -------------------------------------------
# Generate with: openssl rand -hex 32
NC_JWT_SECRET=replace-me-with-32+-bytes-random

# Generate with: openssl rand -base64 32
NC_MASTER_KEY_BASE64=replace-me-with-32-bytes-base64

# -------------------------------------------
# Application
# -------------------------------------------
# Port to expose (default: 3000)
NC_PORT=3000

# Public URL of your instance
# For local: http://localhost:3000
# For production: https://your-domain.com
NC_BASE_URL=http://localhost:3000

# -------------------------------------------
# Docker/OpenClaw
# -------------------------------------------
# OpenClaw Docker image to use
# Options:
#   - ghcr.io/anthropics/openclaw:latest (official)
#   - openclaw:local (build locally with: docker compose build openclaw)
OPENCLAW_IMAGE=ghcr.io/anthropics/openclaw:latest

# Internal Docker network (don't change unless you know what you're doing)
NC_DOCKER_NETWORK=nc-internal
NC_OPENCLAW_PORT=18789
NC_OPENCLAW_DATA_MOUNT=/data
